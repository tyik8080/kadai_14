 
 // 関数 calculateTotal を定義し、引数 num を受け取ります
 const calculateTotal = (num) => {
 // 引数 num の 2 乗を計算し、その結果をコンソールに出力します
    console.log(num**2);
  }
  // 関数 calculateTotal を呼び出し、引数として 10 を渡します
  calculateTotal(10);  // コンソールには 10 の 2 乗である 100 が出力されます